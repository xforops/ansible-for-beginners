Explanation
- groups organize servers
- helps target specific roles

```TXT
[web] → group for web servers
[db] → group for database servers
```

This allows targeting specific servers:
- web group → application setup
- db group → database setup
