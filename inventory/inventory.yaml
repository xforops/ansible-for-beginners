all:
  children:
    production:
      children:
        web:
          hosts:
            web1:
              ansible_host: 10.0.1.10
            web2:
              ansible_host: 10.0.1.11
          vars:
            ansible_user: ansible

        db:
          hosts:
            db1:
              ansible_host: 10.0.1.20
          vars:
            ansible_user: ansible
